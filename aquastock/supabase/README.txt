This folder is for database schema you apply in the Supabase dashboard (or Supabase CLI).

migrations/ — SQL to create tables the first time (see AquaStock/README.md).

Not the same as src/lib/supabase/, which is JavaScript the React app uses at runtime.
