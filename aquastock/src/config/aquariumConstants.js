/** Max displayed / enforced stocking as % of tank bioload capacity (no adds at or above this). */
export const BIOLOAD_PCT_DISPLAY_CAP = 1000;
/** Initial rows shown on the matches (results) species list before "See more". */
export const MATCHES_PAGE_SIZE = 10;
/** How many extra rows each "See more" reveals. */
export const MATCHES_SEE_MORE_INCREMENT = 30;
