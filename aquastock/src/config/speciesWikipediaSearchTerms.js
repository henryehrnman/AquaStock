/**
 * Optional overrides: DB species `name` → Wikipedia search string for photos.
 * Leave empty so the app always uses the name from your SQL row (`name` only).
 */
export const SPECIES_WIKIPEDIA_SEARCH_TERMS = {};
